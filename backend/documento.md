# backend — Backend (IA de conversacao)

Backend do site (Etapa 3): **bot Guto** (LLM de conversacao/chatbot), regras de negocio, APIs, integracao **Supabase** e demais integracoes. Migrar toda a logica aplicavel do backend do site_midia5d para os modulos corretos. Segredos apenas via .env (nunca no Git).
